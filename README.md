# Online Payments Fraud Detection

This project implements a machine learning system for detecting fraudulent transactions in online payments.

I need to know whether the payments is fraud or not, because of that I used the DecisionTreeClassifier to classify dataset.

